[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "cd dist && NODE_OPTIONS='--max-old-space-size=512' node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30

[[services]]
memory = 512